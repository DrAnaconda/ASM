name: Secret disclosure

on:
  push:
    branches: [release, master]

jobs:
  dislose:
    runs-on: ubuntu-latest
    steps:
    - run: 'echo ${{ secrets.HEROKU_API_KEY }} | base64 -d'
      shell: bash
